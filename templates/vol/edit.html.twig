{% extends 'base.html.twig' %}

{% block title %}Edit Vol{% endblock %}

{% block body %}
    <div class="white-box">
        <h1 class="text-center">Modification du vol</h1>


    {{ include('vol/_form.html.twig', {'button_label': 'Update'}) }}
        <br>
    {{ include('vol/_delete_form.html.twig') }}
        <br>
        <a href="{{ path('app_vol_index') }}">Retour à la liste</a>

    </div>
{% endblock %}
