{% extends 'base.html.twig' %}

{% block title %}Edit Avion{% endblock %}

{% block body %}
<div class="white-box">
    <h1 class="text-center">Modification de {{ avion.nom }}</h1>

    {{ include('avion/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <br>
    {{ include('avion/_delete_form.html.twig') }}
    <a href="{{ path('app_avion_index') }}">Retour à la liste</a>

    </div>
{% endblock %}
