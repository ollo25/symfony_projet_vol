{% extends 'base.html.twig' %}

{% block title %}Avion{% endblock %}

{% block body %}
    <div class="white-box">
        <h1 class="text-center">Information de {{ avion.nom}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avion.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ avion.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avion_index') }}">Retour à la liste</a>

    <a href="{{ path('app_avion_edit', {'id': avion.id}) }}">Modifier</a>

    {{ include('avion/_delete_form.html.twig') }}
    </div>
{% endblock %}
